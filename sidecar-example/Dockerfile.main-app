FROM python:3.9-slim

# 设置工作目录
WORKDIR /app

# 复制主应用文件
COPY main-app.py /app/main-app.py

# 设置执行权限
RUN chmod +x /app/main-app.py

# 创建共享目录
RUN mkdir -p /shared/logs

# 暴露端口
EXPOSE 8080

# 启动命令
CMD ["python3", "/app/main-app.py"]
